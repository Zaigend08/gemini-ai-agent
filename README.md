# Gemini AI Agent (Python)

A tool-enabled AI agent built using Google's Gemini SDK.

## Features

- Conversation memory
- Tool calling (calculator + folder creation)
- Modular architecture
- Environment-based API key protection

## Setup

```bash
pip install google-genai python-dotenv
python main.py